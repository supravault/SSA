name: SSA Bundle Scan

on:
  workflow_dispatch:
    inputs:
      project_name:
        description: "Project name (folder under reports/)"
        required: true
        type: string
      project_label:
        description: "Optional label (affects output folder name)"
        required: false
        type: string
      bundle_json:
        description: >
          JSON describing scans. Accepts either:
          1) Array: [{"kind":"wallet","level":3,"target":"0x..."}, {"kind":"fa","level":2,"target":"0x..."}]
          2) Object: {"scans":[...]}
        required: true
        type: string
      pulse_url:
        description: "Optional: URL to Supra Pulse PDF/JSON (Base44 hosted URL or direct)"
        required: false
        type: string
      pulse_tier:
        description: "Optional: Standard | Premium | Spotlight (used for bundle branding)"
        required: false
        type: string

jobs:
  bundle_scan:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    concurrency:
      group: ssa-bundle-${{ github.event.inputs.project_name }}
      cancel-in-progress: false

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build

      - name: Debug dist output (top 200)
        shell: bash
        run: |
          set -euo pipefail
          if [ -d dist ]; then
            find dist -type f | head -n 200
          else
            echo "dist/ does not exist after build!"
            exit 1
          fi

      - name: Run SSA Bundle Scan
        shell: bash
        env:
          SUPRA_RPC_URL: ${{ secrets.SUPRA_RPC_URL }}
          PROJECT_NAME: ${{ github.event.inputs.project_name }}
          PROJECT_LABEL: ${{ github.event.inputs.project_label }}
          BUNDLE_JSON: ${{ github.event.inputs.bundle_json }}
          PULSE_URL: ${{ github.event.inputs.pulse_url }}
          PULSE_TIER: ${{ github.event.inputs.pulse_tier }}
        run: |
          set -euo pipefail

          if [[ -z "${SUPRA_RPC_URL:-}" ]]; then
            echo "❌ SUPRA_RPC_URL secret is missing or empty."
            exit 1
          fi

          project="$(echo "${PROJECT_NAME}" | xargs)"
          label="$(echo "${PROJECT_LABEL:-}" | xargs)"
          pulse_url="$(echo "${PULSE_URL:-}" | xargs)"
          pulse_tier="$(echo "${PULSE_TIER:-}" | xargs)"

          if [[ ! "$project" =~ ^[a-zA-Z0-9._-]+$ ]]; then
            echo "❌ project_name contains invalid characters. Allowed: letters, numbers, . _ -"
            exit 1
          fi

          ts="$(date -u +'%Y%m%dT%H%M%SZ')"
          safe_label=""
          if [[ -n "$label" ]]; then
            safe_label="$(echo "$label" | tr -cd 'a-zA-Z0-9._-')"
          fi

          if [[ -n "$safe_label" ]]; then
            bundle_dir="reports/${project}/${ts}_BUNDLE_${safe_label}"
          else
            bundle_dir="reports/${project}/${ts}_BUNDLE"
          fi

          mkdir -p "$bundle_dir/scans" "$bundle_dir/tmp"

          # Resolve compiled CLI path (robust across tsconfig changes)
          CLI=""
          if [ -f dist/cli/ssa.js ]; then
            CLI="dist/cli/ssa.js"
          elif [ -f dist/src/cli/ssa.js ]; then
            CLI="dist/src/cli/ssa.js"
          else
            echo "❌ SSA CLI not found after build."
            echo "Searched: dist/cli/ssa.js, dist/src/cli/ssa.js"
            find dist -type f | head -n 200 || true
            exit 1
          fi
          echo "✅ Using CLI: $CLI"

          # Normalize bundle_json -> { scans: [...] }
          node - <<'NODE'
          import fs from "node:fs";

          const raw = process.env.BUNDLE_JSON || "";
          if (!raw.trim()) { console.error("❌ bundle_json is empty"); process.exit(1); }

          let parsed;
          try { parsed = JSON.parse(raw); }
          catch (e) { console.error("❌ bundle_json invalid JSON:", e?.message || String(e)); process.exit(1); }

          let scans = [];
          if (Array.isArray(parsed)) scans = parsed;
          else if (parsed && Array.isArray(parsed.scans)) scans = parsed.scans;

          if (!Array.isArray(scans) || scans.length === 0) {
            console.error("❌ bundle_json must be an array or {scans:[...]}");
            process.exit(1);
          }

          const norm = scans.map((s, idx) => {
            const kind = String(s.kind || "").toLowerCase().trim();
            const level = Number(s.level);
            const target = String(s.target || "").trim();

            // Optional allowlist (stored, not enforced unless CLI supports it)
            const modulesAllowlist =
              typeof s.modulesAllowlist === "string" ? s.modulesAllowlist :
              Array.isArray(s.modulesAllowlist) ? s.modulesAllowlist.map(String).join(",") :
              "";

            if (!["wallet","coin","fa"].includes(kind)) throw new Error(`scan[${idx}].kind must be wallet|coin|fa`);
            if (!Number.isFinite(level) || level < 1 || level > 5) throw new Error(`scan[${idx}].level must be 1..5`);
            if (!target) throw new Error(`scan[${idx}].target is required`);
            if (kind === "wallet" && (level < 1 || level > 3)) throw new Error(`scan[${idx}] wallet level must be 1..3`);

            return { kind, level, target, modulesAllowlist };
          });

          fs.writeFileSync("bundle_scans.json", JSON.stringify({ scans: norm }, null, 2));
          console.log(`✅ Normalized ${norm.length} scan(s)`);
          NODE

          mv bundle_scans.json "$bundle_dir/bundle_scans.json"

          report_type="SSA REPORT"
          tier_lc="$(echo "${pulse_tier:-}" | tr '[:upper:]' '[:lower:]')"
          if [[ "$tier_lc" == "premium" || "$tier_lc" == "spotlight" ]]; then
            report_type="SSA FULL INTEGRATED REPORT"
          fi

          pulse_attached=false
          if [[ -n "$pulse_url" ]]; then pulse_attached=true; fi

          cat > "${bundle_dir}/bundle_inputs.json" <<EOF
          {
            "run_id": "${{ github.run_id }}",
            "run_attempt": "${{ github.run_attempt }}",
            "workflow": "${{ github.workflow }}",
            "repo": "${{ github.repository }}",
            "ref": "${{ github.ref }}",
            "sha": "${{ github.sha }}",
            "triggered_by": "${{ github.actor }}",
            "ts_utc": "${ts}",
            "project_name": "${project}",
            "project_label": "${label}",
            "rpc": "${SUPRA_RPC_URL}",
            "report_type": "${report_type}",
            "pulse": {
              "attached": ${pulse_attached},
              "tier": "${pulse_tier}",
              "url": "${pulse_url}"
            },
            "bundle": $(cat "$bundle_dir/bundle_scans.json")
          }
          EOF

          # Download Pulse (CLI supports local path OR URL, but local is more reliable)
          pulse_path=""
          if [[ -n "$pulse_url" ]]; then
            echo "⬇️ Downloading Supra Pulse report..."
            ext="${pulse_url##*.}"
            if [[ "$ext" =~ ^(pdf|json)$ ]]; then
              pulse_path="$bundle_dir/tmp/pulse_input.${ext}"
            else
              pulse_path="$bundle_dir/tmp/pulse_input.pdf"
            fi
            curl -L --fail --retry 3 --retry-delay 1 -o "$pulse_path" "$pulse_url"
            echo "✅ Pulse saved: $pulse_path"
          fi

          # Run scans sequentially
          : > "$bundle_dir/tmp/pdfs.txt"
          total="$(jq -r '.scans | length' "$bundle_dir/bundle_scans.json")"
          echo "▶ Running bundle scans: $total scan(s)"

          for idx in $(seq 0 $((total-1))); do
            kind="$(jq -r ".scans[$idx].kind" "$bundle_dir/bundle_scans.json")"
            level="$(jq -r ".scans[$idx].level" "$bundle_dir/bundle_scans.json")"
            target="$(jq -r ".scans[$idx].target" "$bundle_dir/bundle_scans.json")"
            allowlist="$(jq -r ".scans[$idx].modulesAllowlist" "$bundle_dir/bundle_scans.json")"

            scan_dir="$bundle_dir/scans/${idx}_${kind}_L${level}"
            mkdir -p "$scan_dir"

            cat > "${scan_dir}/scan_inputs.json" <<EOF
            {
              "kind": "${kind}",
              "level": ${level},
              "target": "${target}",
              "wallet_modules_allowlist": "${allowlist}",
              "rpc": "${SUPRA_RPC_URL}",
              "pulse_attached": ${pulse_attached},
              "pulse_tier": "${pulse_tier}"
            }
            EOF

            target_args=()
            if [[ "$kind" == "coin" ]]; then
              target_args=( --coinType "$target" )
            elif [[ "$kind" == "fa" ]]; then
              target_args=( --fa "$target" )
            else
              target_args=( --address "$target" )
            fi

            pulse_args=()
            if [[ -n "$pulse_path" ]]; then
              pulse_args=( --pulse "$pulse_path" )
            fi

            echo "▶ [$((idx+1))/$total] ${kind} L${level} ${target}"
            node "$CLI" scan \
              --kind "$kind" \
              --level "$level" \
              "${target_args[@]}" \
              --rpc "$SUPRA_RPC_URL" \
              --out "$scan_dir" \
              --pdf \
              "${pulse_args[@]}"

            if [[ ! -f "$scan_dir/report.pdf" ]]; then
              echo "❌ Missing expected PDF: $scan_dir/report.pdf"
              exit 1
            fi

            mv "$scan_dir/report.pdf" "$scan_dir/${kind}_report.pdf"
            echo "$scan_dir/${kind}_report.pdf" >> "$bundle_dir/tmp/pdfs.txt"
          done

          # Combine PDFs: cover + all scan PDFs
          echo "🧩 Building combined_report.pdf..."
          python3 - <<'PY'
          import os, sys, subprocess
          from pathlib import Path

          bundle_dir = Path(os.environ["BUNDLE_DIR"])
          pdf_list = (bundle_dir / "tmp" / "pdfs.txt").read_text().splitlines()

          report_type = os.environ.get("REPORT_TYPE","SSA REPORT")
          project = os.environ.get("PROJECT","")
          label = os.environ.get("LABEL","")
          ts = os.environ.get("TS","")
          pulse_tier = os.environ.get("PULSE_TIER","")

          subprocess.check_call([sys.executable, "-m", "pip", "install", "--quiet", "pypdf", "reportlab"])

          from reportlab.pdfgen import canvas
          from reportlab.lib.pagesizes import letter
          from pypdf import PdfReader, PdfWriter

          cover_path = bundle_dir / "cover.pdf"
          c = canvas.Canvas(str(cover_path), pagesize=letter)
          w, h = letter

          c.setFont("Helvetica-Bold", 20)
          c.drawString(72, h - 100, report_type)

          c.setFont("Helvetica", 12)
          c.drawString(72, h - 140, f"Project: {project}")
          if label:
            c.drawString(72, h - 160, f"Label: {label}")
          c.drawString(72, h - 180, f"Timestamp (UTC): {ts}")
          if pulse_tier:
            c.drawString(72, h - 200, f"Supra Pulse Tier: {pulse_tier}")

          c.setFont("Helvetica", 10)
          c.drawString(72, 80, "Generated by SSA Bundle Scan (GitHub Actions).")
          c.showPage()
          c.save()

          writer = PdfWriter()

          for page in PdfReader(str(cover_path)).pages:
            writer.add_page(page)

          for p in pdf_list:
            r = PdfReader(p)
            for page in r.pages:
              writer.add_page(page)

          out_path = bundle_dir / "combined_report.pdf"
          with open(out_path, "wb") as f:
            writer.write(f)

          print("✅ Combined PDF:", out_path)
          PY
        env:
          BUNDLE_DIR: ${{ format('reports/{0}/{1}', github.event.inputs.project_name, '') }}
          PROJECT: ${{ github.event.inputs.project_name }}
          LABEL: ${{ github.event.inputs.project_label }}
          TS: ${{ github.run_id }}
          PULSE_TIER: ${{ github.event.inputs.pulse_tier }}
          REPORT_TYPE: ${{ github.event.inputs.pulse_tier }}

      - name: Upload artifacts (debug)
        uses: actions/upload-artifact@v4
        with:
          name: ssa-bundle-${{ github.event.inputs.project_name }}-${{ github.run_id }}
          path: reports/${{ github.event.inputs.project_name }}/
          if-no-files-found: error
          retention-days: 7

      - name: Commit & Push
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "ssa-bot"
          git config user.email "ssa-bot@users.noreply.github.com"

          git add "reports/${{ github.event.inputs.project_name }}/"

          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "SSA BUNDLE: ${{ github.event.inputs.project_name }} (run ${{ github.run_id }})"
          git push



